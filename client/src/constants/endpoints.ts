const BASE_URL = import.meta.env.VITE_BASE_URL;
const REGISTER = "/register";
const LOGIN = "/login";
const PROFILE = "/profile";
const LOGOUT = "/logout";

export { BASE_URL, REGISTER, LOGIN, PROFILE, LOGOUT };
